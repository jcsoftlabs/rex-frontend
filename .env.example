# Laisser vide tant que le backend n'existe pas : l'application utilise alors
# les données de démonstration de src/api/mock.
# Dès que l'API est en ligne, renseigner son URL et tout bascule sur le vrai backend.
VITE_API_BASE_URL=
